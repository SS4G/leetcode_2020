class Solution:
    def sumZero(self, n: int) -> List[int]:
        cnt = 0
        half = list(range(n - 1))
        last = (n - 1)(0 + n - 2) // 2
        half.append(last)
        return half
            