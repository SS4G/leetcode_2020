class Solution {
    private void traverse(TreeNode root, int[] minAbs, int fatherVal) {
        if (root == null)
            return ;
        else {
            math.abs(rootVal, )
        }
    }

    public int getMinimumDifference(TreeNode root) {
        
    }
}